# Optical-character-recognition---free-symbol-dataset
A repository containing the files dataset.txt (a dataset containing  some  symbols, see dictionnary.txt for more information) and labels.txt for the different labels.  each row of dataset.txt must be reshaped into a 64x64 array to visualize the symbol.  
